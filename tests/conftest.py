"""Shared fixtures for tests."""

import shutil
from pathlib import Path

import pytest

FIXTURES_DIR = Path(__file__).parent / "fixtures"
PROJECT_ROOT = Path(__file__).parent.parent


@pytest.fixture
def tmp_vault(tmp_path: Path) -> Path:
    """Create a temporary vault with basic structure."""
    vault = tmp_path / "vault"
    vault.mkdir()

    folders = [
        "00-INBOX", "01-КОНТРАГЕНТЫ", "02-ДОГОВОРЫ", "03-ПРОЕКТЫ",
        "04-СОТРУДНИКИ", "05-КОНТАКТЫ", "06-ПЕРЕГОВОРЫ", "07-ОПЕРАЦИИ",
        "08-КАЛЕНДАРЬ", "09-СТРАТЕГИЯ/Цели", "09-СТРАТЕГИЯ/Идеи",
        "09-СТРАТЕГИЯ/Ретроспективы", "10-ФИНАНСЫ", "11-БАЗЫ",
        "12-КАНВАСЫ", "13-ШАБЛОНЫ", "14-ВЛОЖЕНИЯ/Фото сотрудников",
        "14-ВЛОЖЕНИЯ/Фото контактов", "14-ВЛОЖЕНИЯ/Документы", "99-АРХИВ",
    ]
    for folder in folders:
        (vault / folder).mkdir(parents=True, exist_ok=True)

    return vault


@pytest.fixture
def sample_company_note(tmp_vault: Path) -> Path:
    """Create a sample company note (type: наша_компания)."""
    note = tmp_vault / "01-КОНТРАГЕНТЫ" / "ООО «Цифратроника».md"
    note.write_text(
        '---\n'
        'title: "ООО «Цифратроника»"\n'
        'type: наша_компания\n'
        'опф_полная: "Общество с ограниченной ответственностью"\n'
        'опф_краткая: "ООО"\n'
        'название_полное: "«Цифратроника»"\n'
        'название_краткое: "«Цифратроника»"\n'
        'адрес: "г. Москва, ул. Примерная, д. 1"\n'
        'инн: "5024251588"\n'
        'кпп: "502401001"\n'
        'расчётный_счёт: "40702810902640001782"\n'
        'банк: "АО «Сбербанк»"\n'
        'бик: "044525225"\n'
        'корр_счёт: "30101810600000000957"\n'
        'tags:\n'
        '  - тип/компания\n'
        '---\n\n'
        '# ООО «Цифратроника»\n',
        encoding="utf-8",
    )
    return note


@pytest.fixture
def sample_counterparty_note(tmp_vault: Path) -> Path:
    """Create a sample counterparty note."""
    note = tmp_vault / "01-КОНТРАГЕНТЫ" / "ООО Пример.md"
    note.write_text(
        '---\n'
        'title: "ООО Пример"\n'
        'type: контрагент\n'
        'инн: "7701234567"\n'
        'опф_полная: "Общество с ограниченной ответственностью"\n'
        'опф_краткая: "ООО"\n'
        'название_полное: "«Пример»"\n'
        'название_краткое: "«Пример»"\n'
        'юридический_адрес: "г. Москва"\n'
        'фактический_адрес: "г. Москва, ул. Тестовая, д. 5"\n'
        'кпп: "770101001"\n'
        'расчётный_счёт: "40702810000000000001"\n'
        'банк: "АО «ТестБанк»"\n'
        'бик: "044525000"\n'
        'корр_счёт: "30101810000000000001"\n'
        'категория: клиент\n'
        'статус: активный\n'
        'дата_создания: 2026-04-05\n'
        'контактные_лица:\n'
        '  - "[[Петров Алексей (ООО Пример)]]"\n'
        'tags:\n'
        '  - тип/контрагент\n'
        '  - статус/активный\n'
        '---\n\n'
        '# ООО Пример\n',
        encoding="utf-8",
    )
    return note


@pytest.fixture
def sample_contract_note(tmp_vault: Path, sample_counterparty_note: Path) -> Path:
    """Create a sample contract note."""
    note = tmp_vault / "02-ДОГОВОРЫ" / "Договор №001.md"
    note.write_text(
        '---\n'
        'title: "Договор №001"\n'
        'type: договор\n'
        'номер: "001"\n'
        'контрагент: "[[ООО Пример]]"\n'
        'сумма: 150000\n'
        'валюта: RUB\n'
        'дата_подписания: 2026-04-05\n'
        'дата_окончания: 2027-04-05\n'
        'статус: активный\n'
        'ответственный_наш: "[[Иванов Иван]]"\n'
        'вид_стороны_1: "«Исполнитель»"\n'
        'вид_стороны_2: "«Заказчик»"\n'
        'должность_подписанта_нашего_рп: "Генерального директора"\n'
        'фио_подписанта_нашего_рп: "Маса Ольги Борисовны"\n'
        'основание_подписанта_нашего_рп: "Устава"\n'
        'должность_подписанта_контрагента_рп: "Генерального директора"\n'
        'фио_подписанта_контрагента_рп: "Иванова Дмитрия Ивановича"\n'
        'основание_подписанта_контрагента_рп: "Устава"\n'
        'спецификации:\n'
        '  - номер: 1\n'
        '    дата: "2026-04-10"\n'
        '    тип: услуги\n'
        '    услуги:\n'
        '      - название: "Годовая поддержка"\n'
        '        отчётность: "Акт о выполненных работах"\n'
        '        стоимость: 150000\n'
        '    место: "г. Москва"\n'
        '    порядок_оплаты: "100% предоплата"\n'
        '    сроки: "1 месяц с момента оплаты"\n'
        '    гарантия: "1 год"\n'
        'docx_файлы: []\n'
        'tags:\n'
        '  - тип/договор\n'
        '  - статус/активный\n'
        '---\n\n'
        '# Договор №001\n',
        encoding="utf-8",
    )
    return note


@pytest.fixture
def sample_employee_note(tmp_vault: Path) -> Path:
    """Create a sample employee note."""
    note = tmp_vault / "04-СОТРУДНИКИ" / "Иванов Иван.md"
    note.write_text(
        '---\n'
        'title: "Иванов Иван Иванович"\n'
        'type: сотрудник\n'
        'должность: "Менеджер"\n'
        'отдел: "Продажи"\n'
        'дата_найма: 2026-01-15\n'
        'статус: активный\n'
        'связи:\n'
        '  - "[[Петров Пётр]]"\n'
        'tags:\n'
        '  - тип/сотрудник\n'
        '  - статус/активный\n'
        '---\n\n'
        '# Иванов Иван Иванович\n\n'
        '## Связи и отношения\n\n'
        '| Лицо | Тип связи | Описание | Период |\n'
        '|------|-----------|----------|--------|\n'
        '| [[Петров Пётр]] | Руководство | руководит | 2026— |\n',
        encoding="utf-8",
    )
    return note


@pytest.fixture
def sample_employee_note_2(tmp_vault: Path) -> Path:
    """Create a second employee note for relationship testing."""
    note = tmp_vault / "04-СОТРУДНИКИ" / "Петров Пётр.md"
    note.write_text(
        '---\n'
        'title: "Петров Пётр Петрович"\n'
        'type: сотрудник\n'
        'должность: "Разработчик"\n'
        'отдел: "IT"\n'
        'дата_найма: 2026-02-01\n'
        'статус: активный\n'
        'связи: []\n'
        'tags:\n'
        '  - тип/сотрудник\n'
        '  - статус/активный\n'
        '---\n\n'
        '# Петров Пётр Петрович\n\n'
        '## Связи и отношения\n\n'
        '| Лицо | Тип связи | Описание | Период |\n'
        '|------|-----------|----------|--------|\n',
        encoding="utf-8",
    )
    return note


@pytest.fixture
def sample_contact_note(tmp_vault: Path) -> Path:
    """Create a sample contact note."""
    note = tmp_vault / "05-КОНТАКТЫ" / "Петров Алексей (ООО Пример).md"
    note.write_text(
        '---\n'
        'title: "Петров Алексей Сергеевич"\n'
        'type: контакт\n'
        'контрагент: "[[ООО Пример]]"\n'
        'роль: "Генеральный директор"\n'
        'статус: активный\n'
        'связи: []\n'
        'tags:\n'
        '  - тип/контакт\n'
        '  - статус/активный\n'
        '---\n\n'
        '# Петров Алексей Сергеевич\n',
        encoding="utf-8",
    )
    return note


@pytest.fixture
def contract_template_path() -> Path:
    """Path to the contract template .docx."""
    return FIXTURES_DIR / "contract_template.docx"


@pytest.fixture
def specification_template_path() -> Path:
    """Path to the specification template .docx."""
    return FIXTURES_DIR / "specification_services.docx"
